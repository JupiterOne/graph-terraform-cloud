---
sourceId: managed:terraform
integrationDefinitionId: '${integration_definition_id}'
questions: 
- id: integration-question-terraform-cloud-users-with-no-multi-factor-authentication
  title: Returns a list of Terraform Cloud users that do not use MFA in a Terraform team workspace or as an individual contributor?
  description:
    Find Terraform Cloud users that do not use mfa when accessing a Terraform team workspace or as an individual contributor.
  queries: 
  - name: bad 
    query: |
      FIND tfe_user AS user
        THAT HAS tfe_organization AS org 
      RETURN
      user.displayName,user.mfaEnabled,user.mfaVerified,org.mfaEnabled    
  tags:
  - terraform-cloud
  - workspace