---
sourceId: managed:terraform
integrationDefinitionId: '${integration_definition_id}'
questions: 
- id: integration-question-terraform-user-with-mfa-disabled-and-mfa-is-not-verified
  title: Which Terraform users do not have MFA enabled and MFA is not verified? 
  description:
    Identify Terraform users that do not have MFA enabled and mfa is not verified.
  queries:
  - name: bad
    query: |
      FIND tfe_user AS user THAT HAS tfe_organization
      RETURN
        user.displayName,user.mfaEnabled,user.mfaVerified    
  tags:
  - terraform