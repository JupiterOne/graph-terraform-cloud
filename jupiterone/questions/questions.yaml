---
sourceId: managed:terraform
integrationDefinitionId: '${integration_definition_id}'
questions: 
- id: integration-question-terraform-cloud-user-that-is-not-mfa-enabled-and-mfa-is-not-verified-within-an-organization-workspace
  title: Which Terraform Cloud users do not have MFA enabled and where MFA is not verified within an organization workspace? 
  description:
    Identify Terraform Cloud users that do not have MFA enabled and where MFA is not verified in an organization workspace.
  queries:
  - name: bad
    query: |
      FIND tfe_user AS user THAT HAS tfe_organization
      RETURN
        user.displayName,user.mfaEnabled,user.mfaVerified    
  tags:
  - terraform-cloud
  - workspace